package de.shingiro;

import java.util.Arrays;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.Future;
import java.util.stream.Collectors;
import java.util.stream.Stream;

/**
 * Hello world!
 *
 */
public class App 
{
    public static void main( String[] args )
    {
        System.out.println( "DROOLS RULEZ!" );
    }

    /*
    {
        //Stream.of(1,2,3).map(n -> Stream.of(n, n*n)).forEach(System.out::println);
        //System.out.println("Summe: " + Arrays.asList(1,2,3,4,5).stream().map(m -> m*m).reduce(0,  Integer::sum));
        Long anzahl = Stream.of(1,2,3,4,5,6,7,8,9,0).collect(Collectors.counting());

        System.out.println("Anzahl: " + anzahl);
    }
    */


}